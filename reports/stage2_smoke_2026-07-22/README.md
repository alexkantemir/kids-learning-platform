# Смоук-тест proxyapi (Claude Sonnet 5) — 2026-07-22

Этап 2, сессия 1 (TASK_STAGE2.md, пункт 1). Один живой вызов на каждую
роль нового пайплайна через proxyapi.ru, модель `claude-sonnet-5`, тема
«Порядок действий в примерах» (историческая проблемная тема — на GigaChat
регулярно давала брак, см. Этап 1).

## Файлы

- `1_content.json` — вызов 1 (генератор контента).
- `2_questions.json` — вызов 2 (генератор вопросов), raw-дампы всех шагов/quiz.
- `3_verdict.json` — вызов 3 (судья) на первом проверяемом вопросе.

## Результат

Вся арифметика в сгенерированном тексте и вопросах — **верна** (проверено
глазами: `(5+3)×2=16`, `4+6×3=22`, `24÷4×2=12`, `3×(8-5)+12÷4=12` — все
значения посчитаны правильно). Судья согласился с автором на первом
проверяемом вопросе (`author_correct=0`, `verifier_correct_index=0`,
`accepted=true`).

## Технический баг, найденный и исправленный по ходу

Первый прогон упал с `KeyError: 'text'` — модель (через этот прокси)
возвращает `content` как массив блоков, и для более длинных промптов
ПЕРЕД текстовым блоком идёт блок `{"type": "thinking", ...}` (extended
thinking). Код в `llm_client.py` брал `content[0]["text"]` по фиксированному
индексу — не работает, если блоков несколько. Исправлено: ищем блок по
`type == "text"`, а не по индексу. См. `app/services/llm_client.py`,
`_proxyapi_generate()`.

## Стоимость (факт, по данным дашборда proxyapi.ru)

| Модель | Запросов | Сумма |
|---|---|---|
| claude-sonnet-5 | 5 | 36,24 ₽ |

Это ВСЕ вызовы claude-sonnet-5 за сессию 1 (два отладочных вызова, которыми
диагностировался баг с `thinking`-блоком, + три ролевых вызова смоук-теста
контент/вопросы/судья) — не только «чистый» смоук-тест из трёх вызовов.
Разбивки по отдельным вызовам дашборд не даёт, но даже все 5 вместе —
36,24₽, то есть на круг один вызов ≈ 7₽. Это заметно ниже той оценки
~2-3₽ за вызов, что закладывалась в расчёт бюджета (~210₽ на 10 тем при
~9-10 вызовах на урок) — возможно, из-за extended thinking (даже пустой
`thinking`-блок мог считаться как выходные токены) или из-за того, что
реальные промпты этого пайплайна длиннее, чем закладывалось в оценке.
**Стоит заложить более высокий фактический бюджет на полный замер в
сессии 3**, чем изначально одобренные 1000₽ формально ещё позволяют
(5 запросов × 7₽ ≈ 35₽ уже потрачено; 10 тем × ~9-10 вызовов × 7₽ ≈
700₽ по этой уточнённой оценке "за вызов" — по-прежнему в рамках
бюджета, но с меньшим запасом, чем казалось из расчёта "за токен").
В сессии 3 — обязательно сохранять `usage` из каждого ответа, чтобы не
полагаться на грубую оценку "средней цены вызова" задним числом.
