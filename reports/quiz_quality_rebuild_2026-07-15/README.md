# Перезамер качества квизов — 2026-07-15

Восстановление утерянной папки `reports/quiz_quality_baseline_2026-07-04/`
(TASK_STAGE1_v4.md, пункт 5 сессии 4). Оригинал не найден ни на одной
машине, ни на сервере — считается утерянным безвозвратно. Эта папка не
воспроизводит точные исторические цифры 27%/50% (сырых данных под ними
больше нет), а строит новое, честное ПАРНОЕ сравнение: старый и новый
пайплайн, одни и те же 10 тем, один день, один движок (GigaChat).

## Файлы

- `generate_test_lessons.py` — генерирует уроки ОБОИМИ пайплайнами
  (старый монолитный `_build_prompt` из `lesson_generator.py`, новый
  `generate_lesson_content -> generate_questions -> verify_and_repair_questions`
  из `lesson_pipeline.py`) на 10 темах из таблицы «Результат по каждому
  уроку» в CLAUDE.md.
- `check_lessons.py` — автоскоринг: настоящий `validate_lesson()` +
  кодовая проверка арифметики quiz (`check_question_arithmetic` из
  `lesson_pipeline.py`, с учётом порядка операций и скобок).
- `lessons_out_old/`, `lessons_out_new/` — сырые сгенерированные уроки
  (10 + 10, JSON).
- `check_output_old.json`, `check_output_new.json` — результат автоскоринга.

## Результат автоскоринга (2026-07-15, GigaChat, temperature по умолчанию)

| Пайплайн | Уроков с браком | Вопросов quiz | Дефектных quiz (авто) |
|---|---|---|---|
| Старый (монолитный) | 6/10 (60%) | 26 | 1 (4%) |
| Новый (generator→verifier) | 0/10 (0%) | 17 | 0 (0%) |

**Это НЕ финальная цифра.** Автоскоринг ловит структурные ошибки
(`validate_lesson()`) и арифметику, вычислимую кодом. Он НЕ ловит
неарифметические дефекты quiz — например, вопрос с двумя одновременно
верными вариантами или без единственного верного (см. методологию ручной
сверки в CLAUDE.md, раздел «БАЗОВАЯ МЕТРИКА КАЧЕСТВА КВИЗОВ»). Именно
такие дефекты давали большую часть исторических 27%: 4% автоскоринга для
старого пайплайна — это нижняя граница, не полная цифра. Ручная вычитка
`lessons_out_old/`/`lessons_out_new/` — следующий шаг (что в оригинальном
плане было сессией 6, здесь — часть «Перезамера» в TASK_STAGE1_v4.md).

Дополнительно найдено и исправлено по ходу генерации (см. журнал сессии 4
в CLAUDE.md): промпт вызова 2 указывал `feedback_correct`/`feedback_wrong`/
`hint` как обязательные только в примере для `multiple_choice` — модель
следовала примеру, а не общему текстовому правилу, и пропускала эти поля
для `fill_blank`/`match_pairs`/`sort_items`. Пример JSON в промпте расширен
на все 4 типа шагов — после этого новый пайплайн ушёл с 100% до 0% брака
по `validate_lesson()`.

## Ручная вычитка (сессия 5, 2026-07-15) — финальные цифры

Поправка к таблице автоскоринга выше: «6/10 (60%) уроков с браком» для
старого пайплайна считало дефектным ЛЮБОЙ структурный сбой где угодно в
уроке (включая шаги вне quiz) — не то же самое, что «уроков с дефектным
вопросом в quiz» (историческое определение из базовой метрики). Только 1
из этих 6 уроков имел реальную проблему в quiz.

После вычитки всех 43 quiz-вопросов вручную (пересчитан каждый ответ,
проверена однозначность):

| Пайплайн | Дефектных вопросов quiz | Уроков с ≥1 дефектным quiz-вопросом |
|---|---|---|
| Историческая базовая линия (2026-07-04, для памяти) | 6/22 (27%) | 5/10 (50%) |
| Старый (монолитный), 2026-07-15 | **5/26 (19%)** | **5/10 (50%)** |
| Новый (generator→verifier), 2026-07-15 | **3/17 (18%)** | **3/10 (30%)** |

### Таблица — старый пайплайн (26 вопросов, 5 дефектов)

| Урок | Вопрос | Вердикт | Причина |
|---|---|---|---|
| 01 Счёт до 10 | Q0, Q1 | OK | — |
| 02 Секреты сложения | Q0 | OK | — |
| 02 Секреты сложения | Q1 | ДЕФЕКТ | «Сумма каких чисел = 56?» — 25+31, 26+30 и 27+29 все дают 56. Explanation ложно утверждает «только 27 и 29» |
| 03 Вычитание до 20 | Q0 | OK | — |
| 03 Вычитание до 20 | Q1 | ДЕФЕКТ | «Какой пример = 14?» — 16-2, 15-1 и 17-3 все дают 14 |
| 04 Умножение на 2 и 3 | Q0, Q1 | OK | — |
| 05 Деление без остатка | Q0 | сомнительно (см. ниже) | |
| 05 Деление без остатка | Q1 | OK | Авто-скоринг сессии 4 дал ложное срабатывание: «делитель в 24:6» = 6 — это верно, автопроверка перепутала с «чему равно 24:6» (=4) |
| 06 Порядок действий | Q0–Q3 | OK | — |
| 07 Половина и Четверть | Q0, Q1 | OK | — |
| 08 Дроби, общий знаменатель | Q0 | OK | — |
| 08 Дроби, общий знаменатель | Q1 | ДЕФЕКТ | «Какие дроби имеют знаменатель 12?» — помечен вариант «3/12 и 7/8», но 8 не делит 12 (частное 1.5); это единственный вариант, который НЕ подходит, два других (denom 4 и 6) как раз делят 12 |
| 09 Периметр/площадь | Q0 | сомнительно (см. ниже) | |
| 09 Периметр/площадь | Q1 | OK | — |
| 09 Периметр/площадь | Q2 | ДЕФЕКТ | «Периметр 10×4?» correct=«24», но 2×(10+4)=28. Собственный explanation тоже пишет «=24» |
| 09 Периметр/площадь | Q3 | OK | — |
| 10 Проценты | Q0 | OK | — |
| 10 Проценты | Q1 | ДЕФЕКТ | «15 от 30?» explanation сам считает 50%, затем пишет «но правильный ответ — 25%» без основания |
| 10 Проценты | Q2, Q3 | OK | — |

### Таблица — новый пайплайн (17 вопросов, 3 дефекта)

| Урок | Вопрос | Вердикт | Причина |
|---|---|---|---|
| 01 Счёт до 10 | Q0 | OK | — |
| 02 Секреты сложения | Q0, Q1 | OK* | *математика верна, но сценарий не встречается в тексте урока |
| 03 Вычитание до 20 | Q0 | ДЕФЕКТ | «Было 19, съел 3, осталось?» correct=«15», но 19-3=16. Explanation сам считает 16, потом пишет «но правильный ответ 15» без причины |
| 03 Вычитание до 20 | Q1 | OK | — |
| 04 Умножение на 2 и 3 | Q0, Q1 | OK | — |
| 05 Деление без остатка | Q0, Q1 | OK | — |
| 06 Порядок действий | Q0, Q1 | OK | — |
| 07 Половина и Четверть | Q0, Q1 | OK | — |
| 08 Дроби (сундучок) | Q0 | ДЕФЕКТ | «Сумма 1/3+1/6?» correct=«1/2», но среди вариантов есть «3/6» — та же величина до сокращения. Два равнозначных верных варианта |
| 09 Периметр/площадь | Q0, Q1 | OK | — |
| 10 Проценты | Q0 | ДЕФЕКТ | «300 от 1500?» correct=«30%», но 300/1500=20%. Explanation сам считает 20%, потом пишет «но правильный ответ 30%, округлили» — 20 и так целое число |

### Главная находка: судья пропускает словесные задачи

2 из 3 дефектов нового пайплайна (уроки 03 и 10) — словесные задачи
(«было X, съел/добавил Y»), не буквальные выражения вида «19-3». Кодовая
проверка арифметики (`check_question_arithmetic`) ищет буквальное
выражение регексом — в словесной формулировке его нет, проверка не
включается, решение целиком уходит судье. Судья — та же модель GigaChat,
что и автор, просто с другим промптом, и она способна на ту же ошибку
(посчитать верно в объяснении, затем самостоятельно передумать на неверный
ответ), что и автор. **Это системная дыра архитектуры, не единичный баг.**

### Отдельно найдено: content-проверки не подключены к продакшену

`check_content_arithmetic()` и `verify_content_consistency()` (сделаны в
сессии 3) не вызываются ни в `generate_and_save_lesson_new()`, ни в
`generate_test_lessons.py` в этой папке. Итоги сессии 4 ошибочно отмечали
пункт «судья проверяет непротиворечивость текста» как выполненный —
функция существует и была протестирована отдельно, но не подключена к
реальному пути генерации. Открытый пункт для следующей сессии.

### Сомнительные случаи (не считаются дефектами выше)

1. Старый, урок 05, Q0: варианты текстово утверждают «остаток 3»/«остаток 1»
   для делений, которые на самом деле выполняются без остатка.
2. Старый, урок 09, Q0: «сложить все стороны» и «сумма×2» — оба
   математически верны, засчитан только один.
3. Новый: несколько вопросов используют числа, которых нет в тексте урока
   — математика верна, но нарушен принцип «строго из текста».
4. Новый, уроки 06 и 09: `sort_items` с формально заданным, но по сути
   несвязным критерием сортировки.

### Passthrough-типы (fill_blank/match_pairs/sort_items)

- Старый: `fill_blank` с несколькими пропусками то плоским массивом
  (структурная ошибка), то массивом массивов неправильной семантики
  (урок 02: пары решений закодированы как независимые по-блочные списки —
  теоретически принял бы неверную комбинацию 20+30≠45).
- Новый: `match_pairs` в уроке 01 придумывает факты, которых нет в тексте
  урока — нарушение архитектурного принципа, ничем не проверяется (эти
  типы вне зоны действия судьи в текущей архитектуре).

## Как запустить заново

Скрипты рассчитаны на запуск ВНУТРИ backend-контейнера (там уже есть
креды GigaChat через docker-compose/.env, не нужно читать .env руками,
как в версии 2026-07-04):

```bash
docker cp generate_test_lessons.py kids-platform-backend-1:/app/generate_test_lessons.py
docker cp check_lessons.py kids-platform-backend-1:/app/check_lessons.py
docker exec -u root -w /app kids-platform-backend-1 python generate_test_lessons.py
docker exec -u root -w /app kids-platform-backend-1 python check_lessons.py
docker cp kids-platform-backend-1:/app/lessons_out_old ./lessons_out_old
docker cp kids-platform-backend-1:/app/lessons_out_new ./lessons_out_new
docker cp kids-platform-backend-1:/app/check_output_old.json ./check_output_old.json
docker cp kids-platform-backend-1:/app/check_output_new.json ./check_output_new.json
```

`-u root` нужен, т.к. `/app` в контейнере принадлежит root, а контейнер по
умолчанию исполняет от `appuser` без прав записи туда.
